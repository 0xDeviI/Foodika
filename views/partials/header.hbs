<header>

    <a href="#" class="logo"><i class="fas fa-utensils"></i> {{name}}</a>

    <div id="menu-bar" class="fas fa-bars"></div>

    <nav class="navbar">
        <a href="/">خانه</a>
        <a href="/signin">ورود</a>
        <a href="/signup">ثبت نام</a>
        <a href="/photos">تصاویر تالار</a>
        {{#if isLoggedIn}}
            {{#if (gt orders 0)}}
            <a style="color: blue;" class="fas fa-shopping-cart" href="/dashboard/orders">{{orders}}</a>
            {{/if}}
        <a style="color: red;" href="/logout">خروج حساب</a> 
        {{/if}}
    </nav>

</header>